<%= form_for(@grid) do |f| %>
  <% if @grid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grid.errors.count, "error") %> prohibited this grid from being saved:</h2>

      <ul>
      <% @grid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :coordiate %><br />
    <%= f.text_field :coordiate %>
  </div>
  <div class="field">
    <%= f.label :ship_id %><br />
    <%= f.number_field :ship_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
